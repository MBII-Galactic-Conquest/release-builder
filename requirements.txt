# Required Python packages for update_bot.py
discord.py>=2.1.0
PyGithub>=1.55
python-dotenv>=1.0.0
paramiko>=3.0.0

# Notes:
# - Versions are minimum recommended; pin to exact versions (==) if you need reproducible installs.
# - Install with: python -m pip install -r requirements.txt
